apiVersion: v1
kind: Service
metadata:
  name: {{ include "short-servic.fullname" . }}
  labels:
    {{- include "short-servic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "short-servic.selectorLabels" . | nindent 4 }}
