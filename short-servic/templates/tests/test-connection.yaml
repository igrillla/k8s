apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "short-servic.fullname" . }}-test-connection"
  labels:
    {{- include "short-servic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "short-servic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
