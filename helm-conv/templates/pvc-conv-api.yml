apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Values.api.name }}
spec:
  accessModes:
    {{- toYaml .Values.api.pvc.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.api.pvc.storage }}